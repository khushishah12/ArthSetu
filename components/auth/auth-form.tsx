"use client";
import { useState, type FormEvent } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { ArrowRight, LockKeyhole, Mail, UserRound } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
export function AuthForm({mode}:{mode:"login"|"signup"}){const router=useRouter();const [error,setError]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");setMessage("");setBusy(true);const data=new FormData(event.currentTarget);try{const supabase=createClient();const email=String(data.get("email"));const password=String(data.get("password"));if(mode==="login"){const{error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;router.push("/app/dashboard");router.refresh()}else{const{error}=await supabase.auth.signUp({email,password,options:{data:{full_name:String(data.get("name"))},emailRedirectTo:`${location.origin}/auth/callback`}});if(error)throw error;setMessage("Account created. Check your email when confirmation is enabled.")}}catch(e){setError(e instanceof Error?e.message:"Authentication failed. Supabase may not be configured yet.")}finally{setBusy(false)}}return <form className="auth-form" onSubmit={submit}>{mode==="signup"&&<label><span>Your name</span><div><UserRound size={16}/><input name="name" placeholder="Mohit Chaudhari" required minLength={2}/></div></label>}<label><span>Email address</span><div><Mail size={16}/><input name="email" type="email" placeholder="you@example.com" required/></div></label><label><span>Password</span><div><LockKeyhole size={16}/><input name="password" type="password" placeholder="Minimum 8 characters" required minLength={8}/></div></label>{error&&<p className="form-error">{error}</p>}{message&&<p className="form-success">{message}</p>}<button className="button-primary auth-submit" disabled={busy}>{busy?"Please wait…":mode==="login"?"Sign in to ArthSetu":"Create ArthSetu account"}<ArrowRight size={14}/></button><div className="auth-separator"><span/>OR<span/></div><Link className="demo-access" href="/api/demo-session?next=/app/dashboard">Explore without an account <ArrowRight size={14}/></Link></form>}
